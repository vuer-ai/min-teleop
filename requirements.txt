mujoco
numpy
scipy
pyzmq                # Approach 2b (dual-process)
aiohttp-cors         # Approach 2b (sim_server CORS)
# Install vuer from local dev:
# pip install -e '/Users/marvin/Code/vuer-ai/vuer[webrtc]'

# --- G1 IsaacLab example (examples/2a_isaaclab_g1.py) ---
# pinocchio and casadi must be installed via conda-forge:
#   conda install -c conda-forge pinocchio casadi
casadi
pyyaml
torch
nlopt
anytree
pytransform3d
trimesh
lxml
# dex_retargeting: install from xr_teleoperate source tree:
#   pip install --no-deps -e path/to/xr_teleoperate/teleop/robot_control/dex-retargeting
# unitree_sdk2py: install from unitree_sdk2_python source tree:
#   pip install -e path/to/unitree_sdk2_python
